SCの再レンダリングを行い現在のCCとRSCペイロードをマージする。（Router Cacheのパージ）

CCでデータを更新し、更新したデータをRSCでfetchしている場合cacheしたRSCペイロードが表示される可能性がある。
そのキャッシュをパージするために使用する。

ただし、dynamicレンダリングの場合画面を表示した時点で必ずSCはレンダリングされるので使う必要はない。（ただしstaleTime設定による）
staticの場合はrouter cache以外のキャッシュが関係しているので別途キャッシュパージが必要。
