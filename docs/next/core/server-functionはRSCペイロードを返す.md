例えばserver function内でrefresh()やredirectをコールすると、SCの再実行を依頼しながらページのRSCペイロードを即返却することができる。

ブラウザはペイロードを受け取るとリクエストを飛ばさずに表示を開始し、ストリーミング待受状態になる。

サーバーはSCの結果をストリーミングする。

つまり、SFの実行から新しい画面表示まで一往復で完結する。

つまりserver functionを使うと、APIを実行してかつページも一緒に作っちゃうという動きになる