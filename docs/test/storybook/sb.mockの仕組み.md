preview.tsxでsb.mockすると、自動的にfn()で置き換えられる。
仮にパスの先に__mock__が存在すればそれに置き換えられる

spyをtrueにした場合は、既存の実装がそのまま使われつつspyになる。
そのまま関数が使われるのでmockする場合はmockResolvedValueなどを使用する必要がある。

## spyの使いどころ
複数exportしているファイルをspyなしでmockするとそのファイルからexportされる全てのファイルがmockされて空っぽになる。
spyであれば全ての関数をspyにしつつ、戻り値を偽装したいものを選択できるのが良い。